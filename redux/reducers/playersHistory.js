let initialHistory = {
  history: []
}


const playersHistoryReducer = (state = initialHistory, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default playersHistoryReducer;